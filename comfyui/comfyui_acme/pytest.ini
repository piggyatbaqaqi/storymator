[pytest]
# importlib mode keeps pytest from prepending directories to sys.path and
# then discovering this pack's own __init__.py as a top-level module
# called "__init__" -- which it would import, pulling in comfy_api and
# defeating the point of acme/ being testable without ComfyUI.
addopts = --import-mode=importlib
# acme/ holds unit tests beside the module they exercise, as
# acme/foo_test.py.  tests/ is the older layout and is being migrated
# there one module at a time; see README.md.
testpaths = acme tests
python_files = test_*.py *_test.py
